pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'print message'
      }
    }
    stage('test') {
      steps {
        echo 'test to be done'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy to be done by chopra'
      }
    }
  }
}